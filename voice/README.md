# Bring your own voice

Put consented script samples in `voice/samples/` and generate your own `voice/voice-profile.json` using `tools/hold-your-voice/hold_voice.py profile --help`. Both are ignored by Git. The public repository contains no private voice corpus. Follow the voice guide before claiming a match.
